{% extends 'base.html.twig' %}

{% block title %}Mon espace - Actual Sport{% endblock %}

{% block body %}
    <h1 class="page-title">👤 Mon espace</h1>

    {# My registrations of sessions #}
    <div class="dashboard-section">
        <h2 class="section-title">📅 Mes réservations</h2>
        {% if registrations is empty %}
            <div class="card">
                <p class="text-muted">Vous n'avez aucune réservations pour le moment.</p>
                <a href="{{ path('app_sessions') }}" class="btn">Réserver une session</a>
            </div>
        {% else %}
            <div class="grid">
                {% for registration in registrations %}
                    <div class="card">
                        <div class="flex-between">
                            <h3>{{ registration.session.course.name }}</h3>
                            <span class="badge badge-{{ registration.status }}">
                            {% if registration.status == 'confirmed' %}
                                Confirmée
                            {% elseif registration.status == 'cancelled' %}
                                Annulée
                            {% else %}
                                {{ registration.status }}
                            {% endif %}
                            </span>
                        </div>

                        <div class="session-details">
                            <div class="detail-item">
                                <span class="detail-label">📅 Date</span>
                                <strong>{{ registration.session.startTime|date('d/m/Y') }}</strong>
                            </div>
                             <div class="detail-item">
                                <span class="detail-label">🕐 Horaire</span>
                                <strong>{{ registration.session.startTime|date('H:i') }} - {{ registration.session.endTime|date('H:i') }}</strong>
                            </div>
                            <div class="detail-item">
                                <span class="detail-label">📝 Inscrit le</span>
                                <span>{{ registration.registeredAt|date('d/m/Y') }}</span>
                            </div>
                            {% if registration.sessionBook %}
                                <div class="detail-item">
                                    <span class="detail-label">🎫 Payé avec</span>
                                    <span>{{ registration.sessionBook.name }}</span>
                                </div>
                            {% endif %}
                        </div>

                        {% if registration.status == 'confirmed' %}
                            {% if registration.session.startTime > "now"|date %}
                                <form method="post" action="{{ path('booking_cancel', {id: registration.id}) }}" class="cancel-form" onsubmit="return confirm('Êtes-vous sûr de vouloir annuler cette réservation ?');">
                                    <button type="submit" class="btn btn-danger btn-small">
                                        ❌ Annuler ma réservation
                                    </button>
                                </form>
                            {% else %}
                                <p class="text-muted">⏰ Cette session est passée</p>
                            {% endif %}
                        {% endif %}
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>

    {# My sessionBooks #}
    <div class="dashboard-section">
        <h2 class="section-title">🎫 Mes carnets de séances</h2>
        {% if sessionBooks is empty %}
            <div class="card">
                <p class="text-muted">Vous n'avez aucun carnet de séances.</p>
                <a href="{{ path('app_shop') }}" class="btn">Acheter un carnet</a>
            </div>
        {% else %}
            <div class="grid">
                {% for book in sessionBooks %}
                    <div class="card">
                        <h3>{{ book.name }}</h3>
                        <div class="book-info">
                            <div class="remaining-sessions">
                                <div class="remaining-number">{{ book.remainingSessions }}</div>
                                <div class="remaining-label">Séances restantes</div>
                            </div>
                            <div class="book-details">
                                <p><strong>Total :</strong> {{ book.totalSessions }} séances</p>
                                <p><strong>Prix :</strong> {{ book.price }}&nbsp;€</p>
                                {% if book.expiresAt %}
                                    <p><strong>Expire le :</strong> {{ book.expiresAt|date('d/m/Y') }}</p>
                                {% endif %}
                                <p class="text-muted">Acheté le {{ book.createdAt|date('d/m/Y') }}</p>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>

    {# My payments #}
    <div class="dashboard-section">
        <h2 class="section-title">💰 Historique des paiements</h2>
        {% if payments is empty %}
            <div class="card">
                <p class="text-muted">Aucun paiement enregistré.</p>
            </div>
        {% else %}
            <div class="card">
                <table class="payment-table">
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th>Montant</th>
                            <th>Méthode</th>
                            <th>Statut</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for payment in payments %}
                            <tr>
                                <td>{{ payment.createdAt|date('d/m/Y H:i') }}</td>
                                <td><strong>{{ payment.amount }}&nbsp;€</strong></td>
                                <td>
                                    {% if payment.sessionBook %}
                                        🎫 Carnet
                                    {% elseif payment.registration %}
                                        📅 Réservation
                                    {% else %}
                                        💳 Paiement
                                    {% endif %}
                                </td>
                                <td>
                                    {% if payment.sessionBook %}
                                        {{ payment.sessionBook.name }}
                                    {% else %}
                                        -
                                    {% endif %}
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        {% endif %}
    </div>
{% endblock %}